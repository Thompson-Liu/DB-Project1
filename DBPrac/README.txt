The top-level class of our code is the Interpreter class in the package named "controllers". 

We used left-deep join. The structure looks like this:

Se()

Further explanation can be found in the code.